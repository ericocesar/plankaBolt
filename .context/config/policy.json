{
  "version": 1,
  "defaultEffect": "allow",
  "rules": [
    {
      "id": "protect-repository-core",
      "effect": "require_approval",
      "when": {
        "paths": [
          "server/**",
          "client/**"
        ],
        "risk": "high"
      },
      "reason": "High-risk changes to core typescript paths require approval."
    },
    {
      "id": "protect-repository-config",
      "effect": "require_approval",
      "when": {
        "paths": [
          ".github/workflows/**",
          "Dockerfile",
          "Dockerfile.dev",
          "docker-compose.yml",
          "package-lock.json",
          "package.json",
          "pnpm-lock.yaml"
        ],
        "risk": "high"
      },
      "reason": "High-risk changes to repository automation and build configuration require approval."
    },
    {
      "id": "block-secrets",
      "effect": "deny",
      "when": {
        "paths": [
          "**/.env*",
          "**/*.pem",
          "**/*.key",
          "**/*secret*"
        ]
      },
      "reason": "Secret-like files are blocked by policy."
    }
  ]
}
